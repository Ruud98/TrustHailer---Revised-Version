# Django 5.2 is the LTS, and it is also the earliest release that runs on
# Python 3.14 — 5.1 crashes rendering any template there (BaseContext.__copy__).
Django==5.2.*
Pillow==11.*
psycopg[binary]==3.2.*
dj-database-url==2.*
python-dotenv==1.*
gunicorn==23.*
# production only
django-storages[s3]==1.14.*
redis==5.*
sentry-sdk==2.*
